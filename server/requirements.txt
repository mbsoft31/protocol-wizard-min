# Core FastAPI dependencies
fastapi>=0.111
uvicorn[standard]>=0.30
pydantic>=2.7
jsonschema>=4.23

# Testing
pytest>=8.3
pytest-asyncio>=0.23
httpx>=0.27  # For async testing

# Optional LLM providers (install only if you have keys)
# Uncomment and install as needed:
# openai>=1.47         # For OpenAI GPT models (with async support)
# google-generativeai>=0.7  # For Google Gemini models

# Optional: Performance monitoring
prometheus-client>=0.20  # For metrics (install if ENABLE_METRICS=true)
# python-json-logger>=2.0  # For structured logging

# Development tools
mypy>=1.8  # Type checking
bandit>=1.7  # Security scanning
safety>=3.0  # Dependency vulnerability checking