# Backend Quickstart

For the complete quickstart, see `server/QUICKSTART.md`.

Summary:
- Install: `python -m pip install -r server/requirements.txt`
- Run dev: `uvicorn server.main:app --reload --port 8000`
- Test: `pytest -q`
- Docker: `docker-compose up -d`

